export default Index: () -> Component {
    @arcen Flex {}
}

// hmm, if we're gonna use the same style, maybe we could also have an arcen::quantii ??

Menu: () -> Component {
    @arcen {
        MenuCard
    }
}
